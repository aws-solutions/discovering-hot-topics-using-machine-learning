{
  "name": "wf-publish-text-inference",
  "version": "2.0.1",
  "description": "Publish inference outputs as events",
  "main": "index.js",
  "scripts": {
    "test": "jest --coverage --silent --verbose",
    "test-debug": "jest --coverage",
    "clean": "rm -rf node_modules"
  },
  "author": "aws-solutions-builder@",
  "license": "Apache-2.0",
  "dependencies": {
    "aws-sdk": "^2.1223.0"
  },
  "devDependencies": {
    "aws-sdk-mock": "^5.7.0",
    "chai": "^4.3.6",
    "jest": "^29.0.3",
    "sinon": "^14.0.0"
  }
}
