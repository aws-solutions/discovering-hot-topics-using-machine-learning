{
  "name": "wf-submit-topic-model",
  "version": "2.1.2",
  "description": "This lambda function submits the topic modeling job. It looks up for the data in the last 48 hours (default) and submits the bucket information for processing",
  "main": "index.js",
  "scripts": {
    "test": "jest --coverage --silent --verbose",
    "test-debug": "jest --coverage",
    "clean": "rm -rf node_modules"
  },
  "dependencies": {
    "aws-sdk": "^2.1354.0"
  },
  "devDependencies": {
    "aws-sdk-mock": "^5.8.0",
    "chai": "^4.3.7",
    "jest": "^29.3.1",
    "sinon": "^15.0.1"
  },
  "author": "@aws-solutions-builder",
  "license": "Apache-2.0"
}